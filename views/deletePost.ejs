<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Eureka | Delete Post</title>
    <%- include("./partials/siteWideStyles") %>
    <link rel="stylesheet" href="/styles/deletePage.css" />
  </head>

  <body>
    <div class="background p-4 min-vh-100">
      <div class="tablet container rounded p-4">
        <nav>
          <a href="/" class="text-white-50"
            ><i class="bi bi-house-fill"></i>Home</a
          >
        </nav>

        <header>
          <h1 class="text-white">Delete <%= itemName %> Post</h1>
        </header>

        <p class="text-light"><%= locals.error %></p>

        <form id="authForm" method="post" class="d-sm-flex align-items-center">
          <input type="hidden" name="itemName" value="<%= itemName %>" />
          <label for="password" class="text-light">Password:</label>
          <input
            type="password"
            name="password"
            id="password"
            class="form-control w-auto mx-sm-1 my-1 my-sm-0"
          />
          <button type="submit" class="btn btn-danger text-white fw-bold">
            <i class="bi bi-trash-fill"></i>Delete
          </button>
        </form>
      </div>
    </div>
  </body>
</html>
